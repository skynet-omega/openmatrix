# Revisión independiente acotada — campaña funcional 16

Revisión del 23-09-2026, exclusivamente de fuentes y archivos existentes. No ejecuté neuronas, GPU, organismo ni nuevos brazos. No modifiqué fuentes o criterios congelados. Inspeccioné contrato, referencias, retirada, restauración, continuación, verificadores y resultados disponibles; reconstruí orientación vertical desde los cuaterniones de las tres referencias terminadas.

**Estado: BLOQUEADO por interrupción externa; etapa 3 todavía no admitida.** No encontré un defecto demostrado del acoplamiento que invalide las tres trayectorias terminadas. La retirada causal y la continuación todavía no tienen resultados, por lo que su corrección en ejecución no está comprobada. Hay omisiones concretas del verificador que conviene corregir en una versión adicional conservando el original.

## Hallazgos priorizados

1. **P1 — La campaña interrumpida no puede obtener PASS ni absorber silenciosamente otro intento.** `PLAN.json:36` limita cuatro referencias y `PLAN.json:39` siete ejecuciones de organismo. `reference_uniform_01/PROGRESS.jsonl:60` acaba en ensayo 20 ms, con 300,977753877989 s acumulados; faltan `RESULT.json`, `traces.npz` y `state_100ms`. Los otros tres brazos consumieron 5.839,763920178986 s. El total conocido es, por tanto, **al menos 6.140,741674056975 s**, no sólo la suma de los recibos completos. `INTERRUPTION.json` conserva correctamente que desapareció el proceso y que la señal exacta no se observó. El registro parcial no demuestra un fallo direccional, numérico o biológico. El verificador final actual exige los archivos faltantes y no debería aprobar esta campaña.

   Recuperación mínima honesta: conservar campaña 16 como incompleta y registrar **un contrato de reparación enlazado**, antes de ejecutar, para un único reemplazo uniforme y los tres ensayos pendientes. Mantener fuentes/modelo, criterios, horizontes, preparación y perfiles; conservar las tres referencias anteriores mediante hashes y declararlas ya expuestas. El total histórico sería cinco intentos de referencia y, si se completa todo, ocho intentos de organismo, aunque sólo siete terminen. Repetir desde `prepared_state` también añade un intento; no recupera el estado perdido a 20 ms. Restaurarlo requeriría además preservar el observador mecánico y las 40 muestras de preparación para conservar el contrato de trazas. Una repetición completa acotada resulta más simple. Registrar por separado el presupuesto nuevo y el gasto total histórico, incluido el parcial como cota inferior si no se puede recuperar su duración exacta. No cambiar retroactivamente los máximos del plan 16 ni llamar ciega a la campaña combinada.

2. **P2 — El verificador comprueba autoconsistencia de las fuentes adicionales, pero no siempre su identidad con la congelación autorizada.** `verify_all.py:43` toma `EXPERIMENT_CONTRACT.json.sources` del propio resultado y verifica sus copias; no lo compara con `EXECUTION_SOURCES.json` o `WITHDRAWAL_SOURCES.json`. Para continuación, `verify_all.py:95` hace lo mismo con `RUN_CONTRACT.json.sources`, sin exigir igualdad con `CONTINUATION_SOURCES.json`. `check_remaining.py:89` informa `same_profile: True` sin validar allí el perfil y el `plan_sha256` del contrato de continuación. Un recibo de otra implementación y sus copias, coherentes entre sí, no quedarían rechazados por esta parte de la verificación. Los runners sí tienen comprobaciones previas (`run_reference.py:19`, `run_withdrawal.py:19`, `run_continuation.py:18`), por lo que esto es una **laguna de verificación independiente**, no evidencia de que se ejecutó otro modelo.

   Comprobación sobre lo existente: en las tres referencias terminadas, los hashes de plan y los mapas de fuentes adicionales coinciden exactamente con los registrados. Corrección mínima: en un verificador nuevo, anclar cada mapa a la congelación registrada, comprobar plan/perfil/intervalo de continuación y añadir corrupción deliberada de esos campos. No necesita repetir simulaciones.

3. **P2 — La postura de referencias y retiradas se acepta desde un canal derivado sin reconstruirla desde la pose.** `check_support.py:12` usa directamente `traces.npz.upright`; el comparador externo comprueba normalización y yaw del cuaternión, pero vuelve a usar el mismo canal para apoyo (`../etapa3_pn629_intervention_20260923_15/chatgpt_verificador_original.py:59`). Alterar el roll/pitch manteniendo yaw y un `upright` antiguo puede eludir esta puerta. El propio `verify_all.py:57` ya aplica la reconstrucción correcta al replay nativo.

   Corrección mínima: exigir `upright == 1 - 2*(qx² + qy²)` con la tolerancia numérica justificada y aplicar el mínimo a la cantidad reconstruida; incluir una corrupción de postura que preserve yaw. **No cambia los resultados actuales:** en derecha, sham e izquierda, las 440 muestras coinciden exactamente; el mínimo reconstruido es 0,9774521903745467, superior a 0,9.

4. **P2 — No queda acreditado el máximo de memoria GPU del contrato.** `PLAN.json:44` y `PLAN.json:45` fijan 18 GiB RAM y 12 GiB GPU. El runner padre comprueba RAM durante los pasos (`../etapa3_pn629_intervention_20260923_15/run_set.py:151`), igual que la continuación (`run_continuation.py:70`); no encontré una comprobación del máximo GPU en estos ejecutores ni un pico total en los tres `RESULT.json`. Los `device_bytes` de componentes no son una medición del máximo total del proceso. `verify_all.py:98` sólo reconstruye el total temporal y luego adjunta todo el presupuesto al resultado. No hay evidencia de exceso; sí falta evidencia para certificar ese máximo. Recuperar telemetría externa si existe o declarar este límite no verificado. Para la reparación, registrar y comprobar el pico sin cambiar las ecuaciones; una medición futura no demuestra retroactivamente el pico de los tres brazos anteriores.

## Lo que los datos permiten y lo que falta

Los terminales de referencia reconstruidos son derecha −0,0588719820°, sham +0,0236864980° e izquierda +0,0928892092°. Hay inversión lateral y separación respecto de sham; falta uniforme completo, y no corresponde anticipar el contraste de cuatro brazos ni la retirada causal. Los recibos de comparación y las pruebas de software no sustituyen esas observaciones faltantes.

El alcance limitado de `PLAN.json:4` está bien explicitado: orientación funcional del modelo con PN629 desactivadas, lector efectivo DNb05 y cuerpo declarado. La retirada angular puede establecer necesidad de ese lector en esta trayectoria acoplada; no identifica una función biológica exclusiva de DNb05. La continuación sólo probaría los observables y el perfil/checkpoint especificados, no equivalencia general del estado oculto. `verify_all.py:100` conserva correctamente el FAIL histórico de esa equivalencia.

Esta campaña no contiene una calibración nueva contra moscas vivas. Una eventual concordancia entre dos integradores del mismo modelo no resuelve diferencias entre preparaciones biológicas, no valida todas las neuronas por disponer de un conectoma grande y no establece navegación, aprendizaje o el sustrato general buscado. Ninguno de estos límites justifica repetir auditorías amplias: lo útil ahora es preservar la interrupción, cerrar las omisiones de verificación sin mover criterios y ejecutar solamente la reparación acotada si se registra como tal.
