El primer intento de replay terminó antes de dar un paso: el script exigía erróneamente `activation_is_probe=True`. El estado preparado real tenía el controlador activo por la ruta de producción (`elapsed_ns=740000000`, mayor que la guarda de 600000000 ns), con `activation_is_probe=False`. No cambió el cuerpo, las ecuaciones, los comandos, las tolerancias de comparación ni el plan. Se corrigió sólo esa precondición y se conservó `body_sham_replay_01/ERROR.json`; la repetición usa directorios `_02` nuevos.
