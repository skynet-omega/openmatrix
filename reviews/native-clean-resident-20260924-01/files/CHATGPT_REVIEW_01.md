# Revisión externa recibida y nueva consulta

La respuesta de la conversación autorizada «Investiga el conectoma de mosca» se leyó después de enviarle el commit `8b336f339be830e6558fe1b4b71058fd7fb2b203`. ChatGPT declaró que leyó los dos archivos de `engine/`, recibos de FP32, SpMM y eventos dispersos, el perfil de llamadores y el cierre de etapa4, pero **no descargó el ZIP** por fallo DNS; tampoco ejecutó CUDA, MuJoCo ni checkpoints. Su revisión es documental y aritmética, no auditoría independiente de la trayectoria.

Coincidió en cuatro puntos que verificamos localmente: el 1,947× FP32 y el 1,119× SpMM son de primitivas; el 19,4996× de `event_sparse` sólo cuenta visitas de la corriente de puerto y su tiempo GPU no es admisible; el controlador CNS anterior ya usa C++ y el ahorro por eliminar Python no se deduce de las 960 llamadas `.get` por ms. Para un runtime general propuso coordinador transaccional C++ por fronteras causales con estados GPU, MuJoCo C y propietarios PN/KC/visión registrando estado, unidades, eventos, versión y operaciones de guardar/restaurar/publicar. Su primer falsador recomendado es rechazo después de trabajo encolado con estado/RNG/eventos emparejados. Un episodio único de 5 s no cierra etapa5 sin control online/replay/perturbación.

La consulta posterior del 24-09 le entregó el [nuevo commit fijado](https://github.com/skynet-omega/openmatrix/tree/4ef6ea724f7d95d84dcab603b9fbdd131e7fc1a1/reviews/native-clean-resident-20260924-01) con código y recibos del controlador residente, incluida la discrepancia de rechazo, y pidió código concreto para `epoch_session` y un discriminador numérico. ChatGPT declaró haber abierto los cinco archivos pedidos y `run_real_one_ms.py`; identificó independientemente el ULP entre `2000e-9` y `duration_ns*1e-9`, el mismo que la traza local y la corrección prospectiva ya probaron. Entregó un discriminador CPU y código propuesto para un coordinador C++ con barreras antes de lectura/restauración, invalidación permanente tras fallo de rollback y serialización en fronteras confirmadas. **No lo compiló ni ejecutó**: informó errores `ClientError` de su entorno. Nuestro fixture C++/MuJoCo se desarrolló y ejecutó localmente; su diseño no acredita por sí solo ese código de ChatGPT ni el conectoma real.

Se envió otra consulta con el [commit que añade el fixture nativo](https://github.com/skynet-omega/openmatrix/tree/65cfc10fe57e5f194e1b621fbf1ba6f81638265d/reviews/native-clean-resident-20260924-01), pidiendo revisión concreta de bugs y primer propietario real. La herramienta confirmó entrega; respuesta pendiente. El usuario confirmó PRO y razonamiento máximo en su interfaz; la herramienta no expone ese ajuste para verificarlo técnicamente.
