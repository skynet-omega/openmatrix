# Revisión adversarial: multirritmo en motor neuronal general

24-09-2026. El usuario propuso separar procesos rápidos (espigas/reflejos) y lentos (corrientes, memoria, navegación) para reemplazar 1.146 recorridos del CSR por 40–60 por milisegundo. Se pide **evaluar**, no ratificar, las cifras ni el valor de esa división para un conectoma completo y otros cerebros.

Datos reales adjuntos al paquete: `trace_real_rhs_RESULT.json` registra 64 RHS efectivamente consumidos en orden de lanzamiento durante el primer tramo de una corrida sham de 1 ms del CNS; el estado final del organismo conserva SHA-256 `4722b051...`. El orden de consultas retrocede 12 veces por las pruebas especulativas del integrador, aunque el avance físico y los eventos tienen reloj. `TRACE_DELTA_01.json` calcula cambios entre consultas: mediana de 72,0 % de las aristas proviene de fuentes que cambian exactamente; con umbral 1e−6 la mediana baja a 1,53 %, pero no hay certificado de error acumulado. `COUPLED_TEMPORAL_FIXTURE_RESULT.json` conserva la variante de relajación temporal de tres neuronas que usó 89 productos contra 4.004 de referencia, pero **falló** su límite fijado de defecto de corriente 0,001. Ese resultado no anticipa que el cerebro completo necesite 40–60 productos.

El código C++/CPU `chatgpt_temporal_cache_probe.py` de la propuesta de ChatGPT pasó su control sintético; con 64 RHS reales y tolerancia de producto absoluta 0,001 por receptor, produjo 26 productos completos + una pasada de preparación, sólo **2,37×** de reducción frente a 64, bajo la puerta prospectiva 10×. En CPU tardó 14,95 s candidato frente a 7,12 s de 64 productos directos; no se ejecutó como motor CUDA. La cápsula de 154 MB no está en este paquete compacto; el recibo contiene SHA-256 `9aabecb6...`, el código de captura exacto `trace_real_rhs_1ms.py` y los resultados por consulta.

Solicito tres respuestas independientes y falsables:

1. Biología: ¿qué mediciones primarias respaldan dos rangos discretos 0,5–5 ms y 100–1.000 ms en mosca? ¿La ruta ORN→PN→LH→DNb05→músculo del cuello está demostrada, o mezcla inferencias con anatomía/efectores? Separar tiempo de canal, neurona, circuito y conducta.
2. Matemáticas: formular al menos tres rutas distintas para bajar productos globales en `q/s` impulsivos **más** transmisión graduada recurrente, con cambios de peso, entradas visuales, eventos SET/ADD con tiempo y retrocesos. Dar defecto/residual que detecte un evento tardío y criterio de fallback. Prohibir dividir el cerebro por nombre anatómico como garantía de error.
3. Ingeniería: con 191 ensayos CNS y 1.146 coeficientes/ms actuales, perfil completo ~3,15 s/ms y meta 0,060 s/ms, identificar el primer experimento en estado real que discrimina la mejor ruta sin portar PN/KC/cuerpo a ciegas. Una implementación genérica debe registrar modelos y puertos, no codificar un solver PN. Cuantificar cómo se alcanzaría ≤42 barridos FP64/ms **y** se reduciría el resto del perfil; no atribuirlo a TFLOPs teóricos.

Declarar archivos que realmente se examinaron y si se ejecutó código. Una recomendación basada sólo en analogía robótica o en duración de memoria no basta. Una respuesta negativa es útil.
