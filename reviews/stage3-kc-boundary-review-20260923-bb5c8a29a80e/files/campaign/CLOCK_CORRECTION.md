El plan congelado y el primer comentario interpretaron `start_elapsed_ns=1,482,375,000` como si la primera discrepancia estuviera a 1,482 ms del estímulo. Es un reloj **absoluto** en segundos: el ensayo empieza en `1,482,000,000 ns`, y ese bloque empieza `375,000 ns = 0,375 ms` después. KC81004 disparó a `500,000 ns` en referencia y `503,125 ns` en causal respecto del inicio. Una revisión de **todos** los eventos aceptados localizó una divergencia registrada todavía más temprana: KC76431 a `98,437/100,000 ns` (referencia/causal). La captura a 1 ms ocurre después de ambas. Por ello el error KC observado a 1 ms no falsifica una contribución del desfase temporal de eventos. No se cambia el plan retrospectivamente; el falso discriminador se marca y se conservan los datos. Para separar causas se necesitaría captura en torno a 98 µs o al final de cada bloque de 125 µs, con neutralidad comprobada y presupuesto nuevo. La primera divergencia **continua** puede anteceder al primer evento registrado.
