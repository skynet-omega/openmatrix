# Campaña44 — sham reproducido, contraste pendiente

**BLOQUEADO por presupuesto CPU excedido.** No se ejecutaron `common` ni `virtual`; no hay resultado del contraste2°/100ms.

Se restauró el checkpoint completo1000ms con igualdad semántica de sesión, prótesis, publicación, operador efectivo y frontera; luego se avanzó el organismo original durante120ms. Los 35 campos de la traza donante coincidieron exactamente en cada intervalo, con sus dtypes numéricos. Se registraron drive/light realmente entregados y4800 fuerzas/órdenes físicas por subpaso. El RNG de brain no cambió durante los120intervalos. La anchura Unicode de `fase` pasó de<U11 a<U6 por la separación de preparación/ensayo; texto idéntico, diferencia de representación conservada. Esta comprobación valida continuidad de los observables durante el sham, no identidad de todo el estado neural en cada instante ni navegación.

El único proceso consumió **466.955220s de pared**, **3169.303591s CPU**, picoRSS **6.910587GiB**. El presupuesto CPU agregado era3000s: exceso **169.303591s (5.64345%)**. El runner congelado omitió CPU de sus guards por intervalo; vigiló pared/RAM/poolGPU. No se registra pico totalGPU separado y no se infiere de un límite. La ronda se detuvo; no se aumentó ningún tope. Un sondeo posterior en otro proceso encontró OpenBLAS14hilos, sin atribuirle causalmente el gasto histórico.

`run_branch.py` y su SOURCE_LOCK permanecen como fueron ejecutados. `budget_guard.py` es una corrección externa posterior: ocho controles de contabilidad pasan normal/-O y la petición real de continuar fue rechazada antes de crear `common_01`, con cero pasos nuevos. Su ruta de sondeo/terminación no se ejerció sobre un organismo; no se declara un guard de grupo/descendientes validado ni una solución al coste. ChatGPT pidió además margen de cierre y pruebas sintéticas multihilo/con hijos antes de promover ese guard.

Se conservan tres rutas futuras distintas: A/medir y fijar hilos dentro del proceso original con comparación fiel y presupuesto propio; B/contabilidad y corte externos, con reservas explícitas y pruebas sin CNS; C/motor11 como estudio numérico separado. Ninguna reabre44 ni modifica2°/100ms para buscar un efecto. Primero falta resolver B y medir coste real de A antes de otra continuación. Las etapas4/5 siguen abiertas; CNS→VNC y seis patas no se integran. El objetivo fuerte de inteligencia no se reduce al sham.

ChatGPT revisó dos resúmenes y declaró no leer NPZ ni comprobar PRO. Sus respuestas originales están conservadas. Jev realizó una clasificación de622tokens de entrada/102de salida; asesoría, no dictamen científico. Cero subagentes Codex. No queda simulación activa.

Desde la raíz, con salida nueva:

```bash
/home/daroch/miniconda3/envs/GPU/bin/python -B campanas/etapa45_neural_contrast_20260925_44/verify_sham.py --folder campanas/etapa45_neural_contrast_20260925_44/sham_01 --out /tmp/sham44_verificacion_nueva.json
/home/daroch/miniconda3/envs/GPU/bin/python -O -B campanas/etapa45_neural_contrast_20260925_44/test_budget_guard.py
```

El paquete de cierre conserva fuentes, contratos, trazas, checkpoint inicial y snapshots completos de inicio/fin, RNG y operador, con manifiesto y contenido deduplicado por SHA256. Su modo completo reconstruye evidencia enCPU desde una extracción nueva; **no reejecuta120ms deCNS**. Los recursos históricos incluidos conservan procedencia y hashes; no se afirma haber validado una nueva ruta portable de simulaciónGPU.
