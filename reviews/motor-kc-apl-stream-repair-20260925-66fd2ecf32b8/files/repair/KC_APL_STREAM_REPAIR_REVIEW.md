# Reparación de orden CUDA y verificación de la lesión KC/APL — 25-09-2026

ChatGPT revisó la sonda anterior y señaló un riesgo concreto: algunas copias `xs[j].copy()`, `target.copy()` y `weights[pos].copy()` se encolaban fuera del stream del oráculo. La sincronización previa no garantiza por sí sola el orden de una copia posterior en otro stream respecto de la consulta siguiente. **No se demostró que la carrera afectara el resultado anterior**; por rigor se reclasificó como provisional y se repitió el brazo afectado bajo un [plan nuevo](KC_APL_STREAM_REPAIR_PLAN_24.json), sin cambiar pesos elegidos, consulta, tolerancias ni preparación.

El [runner v3](probe_kc_apl_same_run_v3.py) encoló copias de entrada, salidas y pesos en `oracle.stream` y sincronizó antes de comparar/exportar. En un organismo real sham de1ms, la consulta2 y la última (59) reprodujeron exactamente los `z,target,rate` consumidos dentro de la misma corrida. Después se anularon698.768 pesos no nulos entre836.605 entradas KC/APL; ninguna coordenada de `target/rate` cambió, y la restauración fue exacta. [Recibo](kc_apl_stream_01/LESION_RESULT.json): PASS_LIMITED,36,55s, dentro del techo120s; cero retune.

Ahora se conservan [arrays completos A/B/A](kc_apl_stream_01/ABA_ARRAYS.npz), incluidos estados y salidas consumidos, outputs de los tres brazos, posiciones y pesos antes/lesión/restauración. Un [verificador separado](verify_kc_apl_stream_lesion.py) recalculó desde ellos 1.437.492 valores `target/rate` por brazo y los836.605 pesos; pasó en Python normal y `-O`, [recibo](kc_apl_stream_01/VERIFY.json). Una bandera `selected_nonzero_weights` corrompida deliberadamente fue [rechazada](kc_apl_stream_corrupt_01/CORRUPTION_TEST.json).

El pase respalda independencia **local de los coeficientes CNS** respecto de esta lesión, en dos estados y tiempos de una sola preparación. No autoriza eliminar el propietario KC/APL, que mantiene otras dinámicas; tampoco se podó el kernel ni se midió velocidad, trayectoria larga, visión o Etapa4/5. El beneficio máximo directo de omitir esas aristas base por barrido sigue siendo3,27%, insuficiente para el factor≥26× integral. ChatGPT propuso además instrumentar el orden de escrituras del propietario; su módulo estaba en un enlace `sandbox:` no accesible desde WSL, de modo que no se atribuye ejecución local de ese código. Se le pidió transferirlo íntegro.
